name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

  build:
    name: Build (client + server)
    runs-on: ubuntu-latest
    # Fake Firebase env vars so Vite can build without real credentials
    env:
      VITE_FIREBASE_API_KEY: ci-placeholder
      VITE_FIREBASE_PROJECT_ID: driiva
      VITE_FIREBASE_APP_ID: 1:000000000000:web:ci-placeholder
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Build
        run: npm run build

  functions-build:
    name: Build (Cloud Functions)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: functions/package-lock.json

      - name: Install functions dependencies
        run: npm ci
        working-directory: functions

      - name: Build functions
        run: npm run build
        working-directory: functions

      - name: Test functions
        run: npm test
        working-directory: functions

  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Run tests
        run: npm test

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck, build, functions-build, test]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    env:
      # ── Staging Firebase public config (driiva-staging project) ──────────
      # These are public Firebase SDK values — safe to inline in workflow.
      VITE_FIREBASE_API_KEY: AIzaSyCl3f6kzImoxUcFFnyET5t-XbjgLwYvX9A
      VITE_FIREBASE_AUTH_DOMAIN: driiva-staging.firebaseapp.com
      VITE_FIREBASE_PROJECT_ID: driiva-staging
      VITE_FIREBASE_STORAGE_BUCKET: driiva-staging.firebasestorage.app
      VITE_FIREBASE_MESSAGING_SENDER_ID: "36820666947"
      VITE_FIREBASE_APP_ID: 1:36820666947:web:f95afe817318a5a79862f6
      VITE_FIREBASE_MEASUREMENT_ID: G-RGXRB5QBEF
      VITE_SENTRY_DSN: https://180774c56396c59418bd1fca9d3c814d@o4510852635099136.ingest.de.sentry.io/4510852639948880
      VITE_FEATURE_AI_INSIGHTS: "true"
      VITE_FEATURE_COMMUNITY_POOL: "true"
      VITE_FEATURE_LEADERBOARD: "true"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Install functions dependencies
        run: npm ci
        working-directory: functions

      - name: Build Cloud Functions
        run: npm run build
        working-directory: functions

      # Build client with staging Firebase config (VITE_* vars injected above)
      - name: Build client (staging)
        run: npm run build:staging

      # ── Firebase ──────────────────────────────────────────────────────────
      # Deploys Firestore rules, indexes, and Cloud Functions to driiva-staging.
      # Requires: FIREBASE_TOKEN secret set in GitHub → Settings → Secrets.
      # Cloud Functions require the driiva-staging project to be on the Blaze plan.
      # If not yet upgraded, change --only to: firestore:rules,firestore:indexes
      - name: Deploy Firebase to staging
        run: |
          npx firebase deploy \
            --only firestore:rules,firestore:indexes,functions \
            --project staging \
            --non-interactive
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

      # ── Vercel ────────────────────────────────────────────────────────────
      # Deploys the pre-built client + Express serverless function to Vercel.
      # Requires these GitHub Secrets:
      #   VERCEL_TOKEN            — from vercel.com/account/tokens
      #   VERCEL_ORG_ID           — from .vercel/project.json or Vercel dashboard
      #   VERCEL_PROJECT_ID_STAGING — Vercel project ID for the staging project
      # Runtime env vars (DATABASE_URL, ANTHROPIC_API_KEY etc.) must be set in
      # the Vercel staging project's Environment Variables settings.
      - name: Deploy to Vercel (staging)
        run: npx vercel --prod --token "$VERCEL_TOKEN" --yes
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_STAGING }}
